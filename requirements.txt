# ============================================================
# Email Triage RL Environment — Grand Finale
# All free, open-source dependencies. No paid API providers.
# LLM calls route through API_BASE_URL + API_KEY (injected).
# ============================================================

# OpenEnv runtime (core server + client protocol)
openenv-core[core]>=0.2.2

# Web framework & ASGI server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data models
pydantic>=2.0.0

# LLM client — OpenAI-compatible API (works with HF router, Groq, LiteLLM proxy)
openai>=1.30.0

# Environment / config loading
python-dotenv>=1.0.0
