
# ============================================================
# Email Triage RL Environment — Grand Finale
# All free, open-source dependencies. No paid API providers.
# LLM calls use API_BASE_URL + API_KEY (injected externally).
# ============================================================

# OpenEnv runtime (core server + client)
openenv-core[core]>=0.2.2

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data models
pydantic>=2.0.0

# LLM client (OpenAI-compatible — works with HF router, Ollama, LiteLLM proxy)
openai>=1.30.0

# Environment / config
python-dotenv>=1.0.0

# Dashboard (optional but included for visibility)
flask>=3.0.0
